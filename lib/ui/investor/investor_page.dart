import 'package:flutter/material.dart';

class InvestorPage extends StatelessWidget {
  const InvestorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Investor Page'),
      ),
    );
  }
}
